# DEPRECATED: Este archivo está marcado como obsoleto
# Usar requirements.DOCKER.txt generado desde Pipfile.lock
# Comando: pipenv requirements > requirements.DOCKER.txt
#
# Para desarrollo local, usar:
# pipenv install
#
# Para Docker, usar:
# pip install -r requirements.DOCKER.txt

# Este archivo se mantiene solo para compatibilidad hacia atrás
# Las dependencias reales están en Pipfile y Pipfile.lock